{
  "additionalProperties": false,
  "properties": {
    "min_score": {
      "description": "cosine similarity threshold; results scoring below it are excluded. Use a value above 1 to match nothing, below 0 to match all. Defaults to 0.5.",
      "type": "number"
    },
    "subject_prefix": {
      "type": "string"
    },
    "text": {
      "type": "string"
    },
    "top_k": {
      "description": "max results to return; defaults to 10.",
      "minimum": 1,
      "type": "integer"
    }
  },
  "required": [
    "text"
  ],
  "type": "object"
}
